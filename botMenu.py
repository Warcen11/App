from kivy.uix.boxlayout import BoxLayout
from kivy.uix.button import Button

botMenu=BoxLayout(height=75, size_hint_y=None)
botMenu.add_widget(
        Button(
            background_color=[1,.5,0,1], 
            background_normal='',
        )
)
botMenu.add_widget(
        Button(
            background_color=[1,.5,0,1], 
            background_normal='',
        )
)
botMenu.add_widget(
        Button(
            background_color=[1,.5,0,1], 
            background_normal='',
        )
)